@model Product

<div>
    <p>@Model.BookName isimli kitabı silmek istediğine emin misin ?</p>
    <form asp-action="Delete">
        <input type="hidden" asp-for="@Model.ProductId" />
        <button type="submit" class="btn btn-danger">Evet</button>
        <a asp-action="Admin" class="btn btn-primary">Hayır</a>
    </form>
</div>