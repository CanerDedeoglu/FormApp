@model Product
@{
    ViewBag.Title = "Details";
}

<div class="row">
    <div class="col-md-8 offset-md-2">
        <h4>@Model.BookName</h4>
        <hr />
        <img src="~/img/@Model.Image" alt="">
        <h6>Sayfa Sayısı : @Model.PageCount</h6>
    </div>
</div>